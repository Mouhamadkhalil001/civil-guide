# Civil Guide - React (no Bootstrap)

This is a simple React project for a civil jobs directory. It uses:

- React and React Router
- Very simple custom CSS (no Bootstrap)
- Static data in a small JavaScript array (no database, no backend)

## How to run

```bash
npm install
npm start
```

Then open http://localhost:3000 in your browser.
